'use strict';
// var config = require('./')
// const morgan = require('morgan');

module.exports = {
  // reloadDelay: 1500,
  proxy: 'localhost:3000',
  port: '8000',
  ui: {
    port: 8001,
  },
  // server: {
  //   baseDir: config.publicDirectory,
  //   middleware: [morgan('dev')],
  // },
  // files: ['public/**/*.html'],
};
